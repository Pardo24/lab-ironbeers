<!DOCTYPE html>
<html lang="en">

    <body>
<h1>Birra</h1>
{{#each beersFromApi}}
<img src="{{this.image_url}}" width="150"/>
<p>{{this.name}}</p>
<p>{{this.description}}</p>
<p>{{this.tagline}}</p>
{{/each}}
        

    </body>
</html>